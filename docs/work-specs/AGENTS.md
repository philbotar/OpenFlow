# Work specs

Numbered design and implementation records for OpenFlow work.

## Rules

- Next free number under `docs/work-specs/` (`001`, `002`, …). Never reuse. Skip `template.md` and `AGENTS.md` when scanning.
- Copy `template.md` → `NNN-short-slug.md`.
- Never write under `docs/superpowers/` or `docs/plans/`.
- Update the File Map in [`../AGENTS.md`](../AGENTS.md) when adding a spec.
